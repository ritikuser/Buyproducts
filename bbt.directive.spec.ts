import { BbtDirective } from './bbt.directive';

describe('BbtDirective', () => {
  it('should create an instance', () => {
    const directive = new BbtDirective();
    expect(directive).toBeTruthy();
  });
});
